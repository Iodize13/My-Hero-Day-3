# AI Usage Disclosure

> ต้องประกาศให้ครบว่าใช้ AI ตรงไหน / prompt อะไร / แก้อะไร
> ไม่ประกาศ = ถือว่า fail

## สรุปการใช้ AI

ใช้ Claude Code (Anthropic) ช่วยงานที่ไม่ใช่เนื้อหาวิชาการโดยตรง — โครงสร้างโปรเจกต์และ git —
ส่วนคำตอบ/analysis ของแบบฝึกหัดใน notebook (3.1-3.6) เป็นของตัวเอง

| ตรงไหน (เซลล์/ไฟล์) | ใช้ทำอะไร | Prompt (ย่อ) | แก้/ปรับอะไรจาก output ของ AI |
|----------------------|-----------|--------------|-------------------------------|
| โครงสร้าง repo (README.md, failures.md, AI_USAGE.md, .gitignore, .env.example) | ให้ร่าง template ไฟล์ตามสเปคใน brief งาน | "scaffold the repo" | ปรับ template ให้ตรงกับ 3 ไฟล์+เกณฑ์ที่โจทย์กำหนด (เช่น ตาราง before/after ใน failures.md, ตาราง AI usage ในไฟล์นี้) |
| `agentic_rag_4hr_homework.ipynb` | ดึงไฟล์ notebook ต้นฉบับจาก repo เวิร์กช็อป (MegaWiz-Dev-Team/agentic_rag_workshop) มาเป็นจุดเริ่มต้น | "fetch & inspect the notebook" | ไม่ได้แก้เนื้อหา notebook — คัดลอกมาทั้งไฟล์เพื่อไปทำแบบฝึกหัดต่อเอง |
| Git setup | ให้ช่วย `git init`, commit, push ขึ้น GitHub | "commit it" / "push it to github" | ตรวจสอบ diff/สิ่งที่จะ commit เองก่อนอนุมัติทุกครั้ง ไม่ commit `.env` จริง |

## กลาง: AI แนะนำอะไร vs ตัดสินใจเองยังไง
ตอน scaffold repo ครั้งแรก Claude ตั้งระดับเป็น "ยาก" ตามที่สั่งตอนนั้น แต่พอทบทวนเวลา/ภาระงานแล้ว
ตัดสินใจเองว่าจะเปลี่ยนเป็น "กลาง" — เป็นการตัดสินใจของตัวเอง ไม่ใช่ AI แนะนำ
Claude ยัง flag ไว้ว่า notebook ต้นฉบับที่ดึงมาไม่มีระบบสุ่มข้อมูล/โจทย์ตามรหัสนักศึกษาตามที่ brief
พูดถึง (`กรอกรหัสนักศึกษาก่อนเริ่ม`) — จุดนี้ต้องตรวจสอบเองว่า notebook เวอร์ชันที่ใช้จริงมีส่วนนั้นหรือไม่
ก่อนเริ่มทำ ไม่ได้เชื่อคำแนะนำของ AI แบบไม่ตรวจสอบ

> ⚠️ ตารางด้านบนคือการใช้ AI ช่วย "งานเตรียม repo" เท่านั้น — ระหว่างลงมือทำแบบฝึกหัด 3.1-3.6
> จริงในสัปดาห์นี้ ถ้าใช้ AI ช่วยจุดไหนเพิ่ม (debug, เขียน prompt, แก้ chunk/top_k ฯลฯ)
> ให้เพิ่มแถวในตารางด้านบนทันที ห้ามลืมประกาศ
